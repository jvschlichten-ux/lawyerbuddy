{
  "name": "lawyerbuddy",
  "version": "1.0.0",
  "description": "LawyerBuddy / Tu Abogado - Secure case tracking app for lawyers and clients",
  "private": true,
  "packageManager": "pnpm@8.0.0",
  "workspaces": [
    "apps/mobile",
    "apps/web",
    "packages/shared",
    "packages/i18n",
    "packages/api",
    "packages/security",
    "backend"
  ],
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "type-check": "pnpm -r run type-check"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
